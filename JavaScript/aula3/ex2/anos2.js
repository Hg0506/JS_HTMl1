const anos = ['2012', '2010', '2020', '2022', '2035', '2045'];

function anosList(anoAtual) {
    
    for (let i = 0; anoAtual <= anos.length; i++) {
        const result = array[index];

    array.forEach(anosList => {console.log[result]});
    }
    
}